# Options: {"version": "1.30.2"}
events {
}

http {
    resolver 8.8.8.8;
    server {
        listen 80;
        server_name example.com;
        location / {
            return 200 "ok";
        }
    }
}
