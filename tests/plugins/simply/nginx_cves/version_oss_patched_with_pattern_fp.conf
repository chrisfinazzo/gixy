# Options: {"version": "1.30.2"}
events {
}

http {
    server {
        listen 80;
        server_name example.com;
        location / {
            rewrite ^/(.*)$ /x?$1 last;
            set $foo bar;
        }
    }
}
