# Options: {"version": "1.29.8"}
events {
}

http {
    server {
        listen 80;
        server_name example.com;
        location / {
            rewrite ^/(.*)$ /x?$1 last;
            if ($args ~ "q=") {
                return 200 "match";
            }
        }
    }
}
