# Options: {"version": "1.30.0"}
events {
}

http {
    resolver 8.8.8.8 1.1.1.1;
    server {
        listen 80;
        server_name example.com;
        location / {
            return 200 "ok";
        }
    }
}
