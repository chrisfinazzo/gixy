# Options: {"version": "1.29.8"}
# Expected: {"issues": 2}
events {
}

http {
    server {
        listen 80;
        server_name example.com;
        location / {
            rewrite ^/(.*)$ /x?$1 last;
            rewrite ^/y /z last;
        }
    }
}
