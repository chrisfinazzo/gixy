# gixy: issue: 1
# Nested quantifier in if condition - no boundary char between (sub)+ iterations,
# so the engine can split a long "sub..." run across outer iterations ambiguously.
server {
	server_name example.com;

	if ($http_referer ~ ^https?://((sub)+)+example\.com$) {
		return 403;
	}
}
