# fp: Every alternation branch ends in a literal that is disjoint from its
# own inner quantifier's char class (`x` not in `[a-w]`, `9` not in `[0-8]`),
# so neither branch can backtrack ambiguously.
server {
	location ~ ^/(foo[a-w]+x|bar[0-8]+9)+$ {
		return 200;
	}
}
