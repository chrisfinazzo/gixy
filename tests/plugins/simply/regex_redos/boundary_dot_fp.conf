# fp: Literal `.` after [a-z0-9-]+ is a safe boundary — `.` is not in the
# inner character class so iterations of the outer `+` cannot ambiguously
# split a run of label chars.
server {
	location ~ ^https?://([a-z0-9-]+\.)+com$ {
		return 200;
	}
}
